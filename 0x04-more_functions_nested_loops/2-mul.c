#include "main.h"
/**
 * mul - function to multiply two integers
 * @b - Integer to be multiplied
 * @a - Integer to be multiplied
 * Return: Always 0
 */
int mul(int a, int b)
{
	int c = a * b;

	return (c);
}
